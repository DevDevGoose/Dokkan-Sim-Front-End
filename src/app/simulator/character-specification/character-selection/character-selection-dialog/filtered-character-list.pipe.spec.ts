import { FilteredCharacterListPipe } from './filtered-character-list.pipe';

describe('FilteredCharacterListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredCharacterListPipe();
    expect(pipe).toBeTruthy();
  });
});
