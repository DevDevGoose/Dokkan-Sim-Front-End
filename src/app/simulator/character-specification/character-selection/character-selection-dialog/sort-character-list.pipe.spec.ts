import { SortCharacterListPipe } from './sort-character-list.pipe';

describe('SortCharacterListPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCharacterListPipe();
    expect(pipe).toBeTruthy();
  });
});
